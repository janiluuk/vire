# Merged automatically by `docker compose` (not committed if you prefer — see .gitignore).
# Passes public URLs into the image build (NEXT_PUBLIC_* is inlined at build).
services:
  web:
    build:
      args:
        NEXT_PUBLIC_SITE_URL: ${NEXT_PUBLIC_SITE_URL:-}
        NEXT_PUBLIC_CALENDLY_EMBED_DOMAIN: ${NEXT_PUBLIC_CALENDLY_EMBED_DOMAIN:-}
        NEXT_PUBLIC_CALENDLY_EMBED_URL: ${NEXT_PUBLIC_CALENDLY_EMBED_URL:-}
